# QR-Adaptor

Paper: **Balancing fidelity and plasticity: Aligning mixed-precision fine-tuning with linguistic hierarchies**

The upstream implementation is integrated under `external/qr_adapter`. TIDAL keeps this method as a method-scoped integration point so the public toolkit layout stays consistent with the paper map.

Primary upstream package:

- `qr_adaptor`

Local upstream path:

- `external/qr_adapter`

