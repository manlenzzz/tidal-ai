# AutoQRA

Paper: **AutoQRA: Joint Optimization of Mixed-Precision Quantization and Low-rank Adapters for Efficient LLM Fine-Tuning**

The upstream implementation is integrated under `external/autoqra`. TIDAL keeps this method as a method-scoped integration point so future toolkit APIs can wrap the search, training, and evaluation components without hiding the original codebase.

Primary upstream package:

- `autoqra`

Local upstream path:

- `external/autoqra`

